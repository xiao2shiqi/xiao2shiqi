# Repository Working Rules

## Git Workflow
- After every code or content change, automatically run `git add`, `git commit`, and `git push` to `origin/main` unless the user explicitly says not to push.

## Blog Title Source of Truth
- For homepage blog list title updates, do not invent or freely translate titles.
- Always use the exact English `title` field from the corresponding post file in:
  - `/Users/phoenix/Documents/workspace/xiao2shiqi.github.io/content/posts/*.en.md`
